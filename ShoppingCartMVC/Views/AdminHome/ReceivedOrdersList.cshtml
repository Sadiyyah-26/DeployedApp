@model List<ShoppingCartMVC.Models.StockOrdersWithIngrVM>
<head>
    <title>
        Supplier Received Orders
    </title>
    <style>
        h1 {
            text-align: center;
        }


        .btn-primary {
            font-size: 20px;
            font-weight: bold;
        }
    </style>
</head>
<br />
<h1> Supplier Received Orders</h1>
<br />
<br />
<table class="table">
    <thead>
        <tr>
            <th>Invoice No #</th>
            <th>Ingredients</th>
            <th>Supplier Name</th>
            <th>Order Date</th>
            <th>Invoice</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.InvoiceId</td>
                <td>
                    <ul>
                        @foreach (var ingredient in item.Ingredients)
                        {
                            <li>@ingredient</li>
                        }
                    </ul>
                </td>
                <td>@item.SupplierName</td>
                <td>@item.OrderDate</td>
                <td>
                    <a href="@Url.Action("AdminInvoice", "AdminHome", new { InvID = item.InvoiceId })" class="btn btn-primary">View</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<br />
<br />